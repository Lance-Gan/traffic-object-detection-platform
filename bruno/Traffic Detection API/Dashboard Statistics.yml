info:
  name: Dashboard Statistics
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/statistics/dashboard?days=30"
  params:
    - name: days
      value: "30"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
