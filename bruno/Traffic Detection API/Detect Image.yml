info:
  name: Detect Image
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/detections/images"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
