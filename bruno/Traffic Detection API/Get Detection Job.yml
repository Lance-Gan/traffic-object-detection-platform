info:
  name: Get Detection Job
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/jobs/{{jobId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
