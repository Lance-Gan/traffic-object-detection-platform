info:
  name: List Detection Jobs
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/jobs?offset=0&limit=20"
  params:
    - name: offset
      value: "0"
      type: query
    - name: limit
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
