info:
  name: System Health
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/health/system"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
