info:
  name: video detect
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/v1/detections/videos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
